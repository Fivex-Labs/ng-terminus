# Registry configuration for GitHub Packages
@fivexlabs:registry=https://npm.pkg.github.com

# For authentication (users will need to add their token)
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN} 